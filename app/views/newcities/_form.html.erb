<%= form_with(model: newcity, local: true) do |form| %>
  <% if newcity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(newcity.errors.count, "error") %> prohibited this newcity from being saved:</h2>

      <ul>
      <% newcity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cityname %>
    <%= form.text_field :cityname %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
